# P4D Work Log — Employee Field Projection by Scope

Date: 2026-07-14

Branch: `codex/mvp-phase4-employee-master`

Required starting HEAD: `c1f347348c19fcc21415b6f83fadbd202ef4743e`

## Checkpoint and scope

The required branch, starting HEAD, and clean worktree were verified before edits. The
authoritative Phase 4 plan, approved P4A-P4C reports, Phase 3 current-assignment/team derivation,
RBAC and live-session construction, P4B Employee 360, P4C account-link/own profile, audit
redaction, request logging, and frontend permission/session-boundary patterns were read before
implementation.

No merge, deployment, push, P4E+ work, sensitive-data field, migration, model, table, encryption
placeholder, reveal contract, or published-revision rewrite was performed. P4D is implemented as a
policy/projection/API/UI change. Phase 3 already defines manager scope as current direct reports;
there is no transitive manager-of-manager scope, and P4D documents and preserves that boundary.

## Central deny-by-default policy

The backend policy is one cohesive boundary split into three small modules:

- `employee_field_policy.py` is the closed field registry, classifications, per-audience
  visibility, and the only phone/birth-date masking implementation.
- `employee_field_projection.py` contains explicit constructors for HR, manager-team, and linked
  employee responses. It calls the central policy for every value and performs no wildcard/model
  copying.
- `employee_projection_contract.py` owns exact recursive response-leaf and ORM-column coverage.
  It rejects missing classifications, excluded fields, Pydantic computed fields, and custom model
  serializers that could otherwise add implicit output keys.

Current Employee, EmployeePersonalProfile, EmployeeEmploymentProfile, and EmployeeAssignment ORM
columns are covered exactly. The registered public employee contracts are EmployeeRead, P4B
EmployeeProfileRead and its mutation responses, the narrowed manager team list, the new manager
profile, and the P4C own state. A persisted or response field addition therefore requires an
explicit classification before focused coverage can pass.

The policy categories are:

- `work_safe`: work identity, status, employment relationship, and safe current organization
  display fields;
- `personal_contact`: phone, birth date, and the masked display payload carrying those values;
- `operational_internal`: tenant/link/assignment/version/history control metadata that is not a
  manager or own-profile field;
- `restricted_future`: policy-only sentinels for `bank_account`, `compensation`,
  `emergency_contact`, `health`, `iban`, `national_identifier`, `passport`, `payroll`,
  `private_address`, `special_category`, and `tckn`. They are not schema or persistence fields and
  have no audience.

## Exact field matrix

### Authorized HR tenant projection — unchanged P4B behavior

- `core`: `id`, `employee_number`, `first_name`, `last_name`, `email`, `status`,
  `employee_version`.
- `personal`: `preferred_name`, raw/null `birth_date`, raw/null `phone`, `version`.
- `employment`: `employment_start_date`, `contract_type`, `work_type`, `version`.
- `organization`: `history_limit`, `history_truncated`, and current/bounded-history assignment
  records containing:
  - assignment `id`, `effective_from`, `effective_to`, `supersedes_assignment_id`,
    `change_reason`, `is_current`, `created_at`, `updated_at`;
  - employee `id`, `employee_number`, `first_name`, `last_name`, `email`, `status`;
  - legal entity/branch/department `id`, `code`, `name`, `status`;
  - position `id`, `code`, `title`, `status`;
  - manager `id`, `full_name`, `email`, `status`, or null.

This projection remains protected by `employee:read:tenant`. Manager team permission does not
authorize it.

### Current direct-manager projection

`GET /api/v1/teams/me` is narrowed server-side to:

- `employee`: `id`, `employee_number`, `first_name`, `last_name`, `preferred_name`, `email`,
  `status`;
- `assignment`: legal entity/branch/department `{code,name}`, position `{code,title}`, and
  `effective_from`.

`GET /api/v1/teams/me/members/{employee_id}/profile` returns:

- `core`: the same seven safe work-identity fields;
- `employment`: `employment_start_date`, `contract_type`, `work_type`;
- `organization.current_assignment`: legal entity/branch/department `{code,name}`, position
  `{code,title}`, manager `{full_name}` or null, and `effective_from`.

Manager responses structurally omit phone, birth date, profile/employee versions, tenant,
membership/global/legacy identity IDs, assignment and organization UUIDs, manager user ID/email/
status, account-link state, assignment history, history controls, supersession/change reason,
timestamps, audit payloads, and every restricted-future sentinel.

### Linked employee own projection

`GET /api/v1/me/employee-profile` remains employee-ID-free as an input and resolves only the live
RequestContext membership/actor link. Its response is:

- outer state: `availability`, linked `employee_id`, and `profile` (all null except availability
  for the uniform unavailable state);
- `core`: `id`, `employee_number`, `first_name`, `last_name`, `email`, `status`;
- `personal`: `preferred_name`, masked-state `birth_date`, masked-state `phone`;
- `employment`: `employment_start_date`, `contract_type`, `work_type`;
- `organization.current_assignment`: legal entity/branch/department `{code,name}`, position
  `{code,title}`, and manager `{full_name}` or null.

For a present value, birth date displays `••••-MM-DD` and phone displays eight bullets plus the
last two numeric digits. A null source becomes `{visibility:"unavailable",display_value:null}`.
There is no unmask endpoint, input, permission, button, flag, or placeholder. Own responses omit
membership/global/legacy identity IDs, manager user ID, all optimistic versions, history, audit,
and restricted-future fields.

## Authorization, query, audit, and error boundaries

- The candidate employee UUID on the manager detail route identifies a resource only. Tenant and
  manager actor come exclusively from RequestContext. The one data query requires tenant match,
  `manager_user_id == actor_id`, `[effective_from,effective_to)` current effectiveness, a
  non-archived employee, and active/on-leave status.
- Unrelated same-tenant, former, future, archived, indirect, cross-tenant, and guessed UUIDs all
  use the same generic assignment-not-found response. Client-provided manager/department/scope
  query or header values are ignored.
- The manager profile is one explicit-column joined `LIMIT 1` query after the two constant
  tenant/feature gates. A fixture with 55 history rows still executes exactly three SELECTs and
  emits no history; selected SQL contains neither phone nor birth date.
- Existing live session, membership, actor, tenant, token-family, and permission-generation checks
  are unchanged and run before the new endpoint.
- No employee-read audit event exists or was added. Manager successes and denials write zero audit
  events. Completion logs and generic errors receive no profile/query/header values. Focused tests
  inject phone, birth date, and a unique value for every restricted-future name and prove none is
  present in manager responses, denial envelopes, completion records, or audit state.

## Frontend product

- Dashboard `Ekibim` consumes only the narrowed `/teams/me` contract and links each current direct
  report to `/team/{employeeId}`.
- The `/team` layout checks `employee:read:team` before mounting the data client. The responsive,
  accessible detail screen calls only the manager-safe endpoint and includes loading, empty,
  retry/error, and safe work/organization states.
- Both manager list and detail clients validate exact response keys. Detail rejects a response
  whose `core.id` differs from the route employee. Requests bind session generation, user,
  membership, tenant, permission generation, permission, and route employee; delayed successes,
  failures, and pagination from an old boundary are ignored.
- Employee 360 behavior is unchanged. `Profilim` consumes the centralized masked own contract,
  explains masked/unavailable values, and contains no reveal control. Its client rejects extra
  keys and inconsistent outer/profile employee IDs.
- Proxy coverage now includes `/team/:path*`; role-aware layouts and navigation prevent
  unauthorized data-client mounting.

## Focused evidence

### Backend policy/service/API/audit/query and compatibility

```text
uv run pytest -q backend/tests/test_employee_field_policy.py backend/tests/test_manager_team_profile_service.py backend/tests/test_manager_team_profile_api.py backend/tests/test_employee_profile_api.py::test_get_employee_profile_returns_the_exact_employee_360_aggregate backend/tests/test_employee_profile_service.py::test_employee_profile_read_aggregates_profiles_and_phase3_history_boundedly backend/tests/test_employee_account_link_schemas.py backend/tests/test_employee_account_link_service.py backend/tests/test_employee_account_link_api.py backend/tests/test_employee_assignment_api.py::test_hr_assigns_changes_and_reads_history_while_team_scope_is_derived
47 passed in 16.47s
```

The 47 tests comprise 20 new P4D policy/service/API tests, 24 targeted P4C schema/service/API
compatibility tests, two targeted P4B Employee 360 aggregate/bounded-query checks, and one Phase 3
assignment/team compatibility check. They cover the explicit projection matrix, direct endpoint
bypasses, permission separation, own guessed-ID behavior, exact schema/ORM registries,
unclassified and implicit-serialization fail-closed behavior, no wildcard/model dump projection,
all restricted sentinel values, audit/log/error non-leakage, and bounded manager SQL.

Isolated new-endpoint smoke:

```text
uv run python scripts/p4d_api_smoke.py
P4D_API_SMOKE_OK
```

### Python and frontend static gates

- Ruff check over all 17 changed/new Python files: passed.
- Ruff format check over the same 17 files: `17 files already formatted`.
- `npm run lint`: passed.
- `npm run typecheck`: passed.
- `git diff --check`: passed before the work-log addition and is repeated at handoff.

No frontend production build was run.

### Focused browser role journey

```text
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/opt/hermes/.playwright/chromium_headless_shell-1228/chrome-linux/headless_shell npx playwright test tests/employee-field-projection-flow.spec.ts --config=playwright.p4d.config.ts --project=chromium
1 passed (27.5s)
```

The one dev-server journey rotates HR to manager A to manager B to employee. It proves HR retains
raw approved personal fields; a manager list/detail contains safe fields and no personal labels or
values; a delayed manager-A response cannot overwrite manager B; unrelated and response-ID-
mismatched routes render no stale employee; an employee direct `/team` route makes zero manager
requests; and `Profilim` is masked/unavailable, makes no manager/HR request, ignores an arbitrary
query employee ID, and exposes no reveal control.

During development, the first run exposed Next dev-mode duplicate reads in an overly exact test
counter, and the second exposed the five-second default expectation timeout during cold route
compilation. The harness now permits duplicate same-scope reads while retaining exact zero-cross-
scope assertions and uses a bounded 15-second expectation/90-second test timeout. The unchanged
single journey then passed.

### Repository and migration state

- Branch and pre-commit HEAD remained the required branch and
  `c1f347348c19fcc21415b6f83fadbd202ef4743e`.
- `git diff --name-only -- backend/alembic backend/app/models` returned no paths.
- `uv run alembic heads` returned the sole `0034_p4c_employee_account_links (head)`.
- No persistence change exists, so the conditional focused PostgreSQL lane was not applicable.

## Broad gates intentionally skipped

Per the P4D intermediate-block strategy, the following were not run and are not claimed:

- full `pytest`;
- full PostgreSQL marker lane or a new PostgreSQL persistence proof;
- broad OpenAPI/snapshot regression (the focused test inspects the exact new/own operations);
- broad backend smoke/inventory;
- full frontend production build;
- broad Playwright or existing full browser suites;
- P4H exhaustive regression/security/performance/query-budget gates.

## Residual risks and deferred proof

- The browser journey mocks HTTP; backend authorization/projection behavior is separately proven
  by focused ASGI/service tests and executable smoke rather than one real-token browser stack.
- `/teams/me` is intentionally narrowed from the prior oversized assignment response. The in-repo
  manager client and targeted Phase 3/P4D compatibility tests are updated, but a broad external-
  consumer/OpenAPI regression is deferred to P4H by instruction.
- Own masking deliberately retains birth month/day and the final two phone digits. This is an
  explicit product policy, not encryption or unmasking, and can be made stricter later without a
  reveal contract.
- Phase 3 PostgreSQL already proves current direct manager scope and its index. Because P4D changes
  no persistence, that lane was not rerun under the conditional test strategy.
- Broad auth-generation, frontend production-build, and complete API-registry regressions remain
  P4H evidence; the implementation continues to use the approved live-session and permission
  boundary unchanged.

## Independent review closure

Read-only backend review found no authorization or current-data leak. It identified two evidence/
coverage gaps, both closed before final verification:

- recursive response coverage now rejects Pydantic computed fields and custom model serializers,
  with synthetic regression proofs;
- manager audit/log/error evidence now derives and injects every restricted-future name and a
  unique value from the central registry, including `special_category` and `tckn`.

The reviewer confirmed the three-module split is cohesive and that manager derivation/query,
narrowed list, own identity/masking, HR compatibility, generic errors, and read-audit behavior are
sound.
